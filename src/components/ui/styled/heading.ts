"use client";

import { ark } from "@ark-ui/react/factory";

import { styled } from "$:styled-system/jsx";
import { heading } from "$:styled-system/recipes";

export const Heading = styled(ark.h4, heading);
